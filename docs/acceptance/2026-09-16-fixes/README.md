# 已知问题修复与复测

基线：main `49852e8`；修复分支：`fix/acceptance-known-issues`。
范围与验收门禁见 [修复契约](../../acceptance-fixes-contract.md)，原问题见 [首次验收](../2026-09-16-real-api/README.md)。
本次为源代码修复，未重新制作客户安装包。

## 结果

| 项目 | 修复 | 验证结果 |
| --- | --- | --- |
| F01 来源 | 移除统一聊天中冲突的“无工具权限”提示；明确实际检索要求；工具模式在发送正文前核对所有正式 `[S数字]` 引用 | 无工具/失败工具/错误编号/跨轮沿用来源全部拦截，不流出正文、不存历史、不重试；真实消防通道问题调用 search_knowledge，返回唯一真实 S1，明确目录未提供宽度数值 |
| F02 多轮 | 历史回答以 answer JSON 封装，不补造历史业务字段；转义后的实际长度计入上下文预算 | 概念解释→缩短→生日祝福三轮通过；11160字合成资料后追问返回“海蓝-FIX-0926”；无工具时仍实时输出 |
| F03 新会话 | 仅非空新建标识触发清空，保存时从 new 切换到 chat 不再重置 | 真 Vue Router 回归通过；真实浏览器发送后无需刷新即保留回答，刷新恢复相同会话，显式新建仍清空 |
| F04 工单 | auto 的四类现场问题可保留明确要求人工复核的 human_review；未诊断字段随申请保留 | 后勤、质量、管理样例首轮均 can_propose=true；浏览器自动进入后勤申请并生成待确认提案；正式记录数仍为0 |

高风险样例为 emergency；用户声称“应该没事”后仍 emergency、risk_retained=true，保留避险正文与人工复核。
unknown、普通咨询、collect_more_info、未要求人工复核的缺失信息结果仍不能申请；旧 consult 行为不变。

## 验证与成本

- 修改前定向基线：100 Python / 43 前端通过。
- 新增回归在修复前出现8项后端失败、1项路由行为失败，均与目标问题吻合；修复后通过。
- 最终 `scripts/verify.ps1`：539 Python / 44 前端通过，TypeScript 与 Vite 构建通过；既有 Pydantic 弃用警告1项。
- 真实服务：DeepSeek V4 Flash、快速模式；11条 API 合成请求、2条浏览器请求，共14次模型请求（含1次检索后续生成）。
- SDK usage：58983 tokens（输入52324、输出6659）；14次均返回 usage。没有自动收费重试，没有新增视觉调用。
- 资料和持久化全部位于独立8058验收实例，未使用真实项目或人员资料；正式工单数0。
- 原始请求、NDJSON、结果、SDK元数据及浏览器状态归档于 [results.jsonl](results.jsonl)，每行一个带 kind 的证据对象；不包含密钥或隐藏思维链。
- 全量离线日志另保留在工作区 `real-api-fix-verification-20260916/offline-verification.txt`，SHA256：`83cd3a252c181081016b5fa784d80007328e9be9294c602f8798818f83ed12b9`。

## 边界与后续

工具模式现在等正文和来源校验完成后再发送正文，工具进度仍实时返回。这是防止未验证引用提前显示的延迟取舍。
来源检查保障正式 `[S数字]` 标记存在于本次真实返回的来源集合，不证明正文每个结论均受来源支持，
也不检测所有不带编号的虚假检索断言；不宣称消除了开放域幻觉。无工具模式不提供来源核验保证。
一次真实知识咨询的辅助 analysis 未通过业务校验，按既有隔离契约保留正文、标记 unavailable、关闭工单资格；
其真实来源与工具结果仍保留。这不是整轮失败，也不计为有效业务分析。
多轮格式修复已通过原路径抽样，模型仍可能产生无效 JSON；原有拒绝、恢复输入和手动重试边界保留。
未配置 Tavily，本次不宣称真实网络搜索通过。合成样例不代表工程诊断、法规适用或全部真实场景的质量评估。
