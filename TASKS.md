# 安全质量 Agent 工作清单

## 已知问题修复（2026-09-16）

用户已授权“改进已知问题，继续优化”。范围、教程依据与人工门禁见 [修复契约](docs/acceptance-fixes-contract.md)。
结果与边界见 [复测报告](docs/acceptance/2026-09-16-fixes/README.md)；下方原验收保留历史问题描述。

- [x] F01：增加工具模式正式来源编号的确定性拦截；正文校验前不外发，真实消防通道检索复测通过。
- [x] F02：统一聊天历史采用 JSON 答案封装，并计算转义后的上下文预算；转题和长资料追问复测通过。
- [x] F03：保存新会话不再清空回答；真路由回归与浏览器保存/刷新验证通过。
- [x] F04：待人工核验申请不再被维修诊断缺失项阻断；未知风险、人工确认和高风险保留不变。
- [x] 全量539 Python、44前端测试及生产构建通过；新增16项后端和1项路由回归。
- [ ] 继续评估无编号检索断言与引用正文的语义支持；一次真实知识咨询的辅助分析降级已记录。
- [ ] 配置 Tavily 后完成真实网络搜索验收；客户安装包尚未重新打包。

## 真实 API 验收归档（2026-09-16）

目标：归档 d31d4fb 的真实服务验收及可复现问题；教程依据：第7章原生工具、第9章上下文、第12章评估与 Extra09 留痕。
当前行为与验收条件：真实服务部分通过，不能判为完整验收通过；详见 [报告](docs/acceptance/2026-09-16-real-api/README.md)。
允许修改：报告、脱敏证据、工作清单与验证索引；禁止修改：产品代码、凭据、真实数据、依赖与发布包。
测试命令：scripts/verify.ps1、git diff --check、报告链接和秘密值扫描。
人工确认点：用户已明确授权上传并合并本次文档；工单及工程结论门禁不变。

- [x] 35次真实文字请求、1次合成图片调用；归档版本、usage、耗时、截图与脱敏请求。
- [x] 保存停止重试、持久化、真实工具与隔离工单闭环证据；不上传运行数据库或凭据。
- [ ] F01 / P1：未检索却生成检索结论和来源编号。
- [ ] F02 / P1：多轮话题切换和长上下文追问返回非JSON，整轮失败。
- [ ] F03 / P1：新建聊天成功绑定会话后，界面被重置为空。
- [ ] F04 / P2：普通跟进事项持续追问，难以触发待人工确认申请。
- [ ] 配置 Tavily 后完成真实搜索验收；本轮真实搜索调用为0。

以下保留各阶段历史记录，以顶部修复节和复测报告为最新结论。

- [x] PR #28：在功能分支解决与main的压缩合并历史冲突，保留现有功能；不自动合并主线。

## 当前：持久聊天与受控联网（2026-09-13）

契约：`docs/persistent-chat-tools-contract.md` C01–C09；基线 f0aacd3，独立分支 feat/persistent-chat-web-tools。

- [x] 核对 beta.3 基线，482项离线 Python 回归通过。
- [x] 原子持久会话、刷新/重启恢复、幂等及上下文预算。
- [x] 核验 Codex 公开源码版本/许可，接入最小原生联网工具与来源。
- [x] DeepSeek/Tavily独立配置与真实工具状态、桌面/移动隔离界面验收。
- [x] 505项Python、43项前端及构建通过，客户说明与配置缺口已记录。

仅本地改动；腾讯分支未合并，真实付费模型/搜索待授权验收，未重新打包发布。

- [x] 按项目补齐 search_knowledge/calculate，与联网/网页/时间组成5项原生工具；523项Python与43项前端通过。
- [x] 已提交并推送功能分支，创建 PR #27：https://github.com/Arien-LY/sea-son-safety-agent/pull/27；基线 fix/unified-chat-regression，未合并。

## 当前：聊天校验失败隔离与比赛包更新

契约：`docs/chat-answer-isolation-contract.md` I01–I06。

- [x] 正文独立Pydantic校验；分析无效保留正文，不自动补调模型。
- [x] 降级不沿用旧工单资格；已有高风险仍保留，旧consult边界不变。
- [x] 客户展示降级提示；第三方声明收纳到内部目录，原始许可保留。
- [x] 482项Python、39项前端行为测试及构建通过。
- [x] 干净版本v0.1.0-beta.3构建、独立包验证与GitHub上传核验。

发布：私有仓库v0.1.0-beta.3，源提交912d6b5；4135文件散列、原生启动、双类别闭环、
重启持久化与GitHub下载SHA256复核通过。旧beta.1保留；没有合并main或PR。

## 历史：统一聊天回退修复

契约：`docs/chat-regression-contract.md` R01–R07。

- [x] 统一入口恢复完整逐轮问答，并覆盖截图中的切换问题场景。
- [x] 默认快速回复、可选深度思考，按实际请求展示状态。
- [x] 供应商正文真流式，失败无历史/工单副作用，高风险边界不变。
- [x] 467项Python、38项前端、构建和1280/390×844隔离界面验证通过。
- [x] 本地v0.1.0-beta.2修复试用包独立解压、4134文件散列及双类别闭环验证通过。

此包为未提交工作区的本地试用构建（dirty_build=true），不能冒称已发布正式包；
上述本地修复完成时GitHub仍是v0.1.0-beta.1，当次未提交、推送、合并或上传。真实模型质量/速度未抽测。

## Windows 客户试用版发布

目标、教程依据、范围、测试和人工门禁见 `docs/windows-release-contract.md` P01–P10。

- [x] 原生启动器、加密模型设置、内置运行时和仅本机静态服务。
- [x] 去除客户界面的教程/开发字段，保留必要安全提示与中文业务字段。
- [x] 白名单打包、447项Python/35项前端回归、独立解压原生启动与双类别闭环验收。
- [x] GitHub私有比赛Beta发布并核对下载资产：`v0.1.0-beta.1`，ZIP下载复算SHA256一致。

原则：一次只推进一个阶段；每个阶段同时通过自动测试和人工验收后，才能进入下一阶段。

## Phase 0：干净仓库与教程基线

- [x] 从旧项目 `origin/main` 选择性迁移通用底座。
- [x] 初始化独立 Git 仓库，不携带旧仓库提交历史和运行数据。
- [x] 固定 Python 3.12、Vue 3、FastAPI 和 Hello-Agents 0.2.9。
- [x] 建立 `AGENTS.md`、架构、复用清单和 ADR。
- [x] 建立后端健康检查、前端起始页和完整验证脚本。
- [ ] 两位队员在各自电脑执行 `setup.ps1` 和 `verify.ps1`。

验收：新克隆后可以独立安装；`pytest` 和前端生产构建通过；没有真实密钥或旧运行数据。

## Phase 1：文字咨询与结构化问题分析

教程依据：第 4、7 章。

- [x] 定义 20 条固定案例：普通咨询、安全、质量、管理、后勤各 4 条（契约与验收说明见
  `docs/phase1-acceptance-cases.md`）。
- [x] 定义输入契约：文字描述、可选项目/区域/角色，不含图片二进制（见
  `docs/phase1-text-input-contract.md`）。
- [x] 完成单 Agent 基础对话，普通问题直接回答（见 `docs/phase1-basic-dialog.md`）。
- [x] 完成 `IssueAnalysis` JSON Schema 输出（见 `docs/phase1-issue-analysis-schema.md`）。
- [x] 分类支持 `safety/quality/management/logistics/consultation/unknown`（见
  `docs/phase1-analysis-categories.md`）。
- [x] 风险支持 `undetermined/low/medium/high/emergency`（见
  `docs/phase1-analysis-risk-levels.md`）。
- [x] 信息不足时输出缺失字段，不猜测地点、人员和责任（见
  `docs/phase1-completion.md`）。
- [x] 高风险输出立即避险提示和人工复核标记（见 `docs/phase1-completion.md`）。
- [x] 用 Fake LLM 测试正常、空输入、非法 JSON、超时和模型错误（见
  `docs/phase1-completion.md`）。

验收：固定案例分类与路由达到团队约定阈值；无 Task 副作用；真实模型冒烟单独记录。

状态：Phase 1 实现清单已完成；专业人员人工验收和经明确授权的真实模型冒烟仍是进入下一阶段前的
独立门禁，不能由 Fake LLM 协议测试替代。

## Phase 2：受控问题上报工具

教程依据：第 7 章 FunctionCallAgent 与工具系统。

- [x] 只增加一个 `propose_issue_record` 工具（契约见
  `docs/phase2-propose-issue-record-contract.md`）。
- [x] 工具接收经过校验的结构化问题，不直接保存或派单（见
  `docs/phase2-validated-analysis-tool-boundary.md`）。
- [x] 页面显示“建议创建记录”，由用户补充和确认（见 `docs/phase2-completion.md`）。
- [x] 确认前后数据差异可见（见 `docs/phase2-completion.md`）。
- [x] 工具返回统一 `ToolResult`，覆盖参数错误、重复调用和内部错误（见
  `docs/phase2-completion.md`）。
- [x] 记录工具名、参数摘要、结果、时间和错误码，不记录隐藏思维链（见
  `docs/phase2-completion.md`）。

验收：普通咨询不调用工具；只有明确需要留痕/跟进的问题出现记录建议；未确认不落库。

状态：Phase 2 实现清单已完成；页面、错误矩阵和脱敏审计已通过自动及本地浏览器验收。正式落库、
派单和整改状态由 Phase 3 的确定性工作流承接；提案确认本身仍不直接产生持久化副作用。

## Phase 3：整改和后勤工作流

教程依据：第 4 章 Plan-and-Solve 的分步思想，状态推进由确定性代码负责。

- [x] 冻结状态：`draft → submitted → assigned → rectifying → pending_review → closed`（见
  `docs/phase3-workflow-contract.md`）。
- [x] 定义取消、驳回、补充信息和重新整改路径（见 `docs/phase3-workflow-contract.md`）。
- [x] 创建本地持久化 Store，并写并发/原子写入测试（见 `tests/test_phase3_workflow.py`）。
- [x] 实现责任角色建议，但禁止 AI 最终归责（见 `docs/phase3-workflow-contract.md`）。
- [x] 整改人提交说明，复查人确认关闭（见 `tests/test_phase3_api.py`）。
- [x] 高风险问题禁止提交人自行关闭（见 `tests/test_phase3_workflow.py`）。
- [x] 后勤工单复用相同生命周期，只使用不同问题类型和责任角色（见
  `tests/test_phase3_api.py`）。

验收：一条安全问题和一条后勤问题可以从上报完整走到关闭，所有状态变化可追踪。

状态：Phase 3 实现清单已完成；状态契约、例外路径、本地 Store、权限矩阵、安全/后勤端到端流程已
通过自动及本地浏览器验收。真实身份鉴权、跨进程并发、外部派单和通知不在本阶段范围。

## Phase 4：知识检索与依据引用

教程依据：第 8 章 RAG、第 9 章上下文工程。

- [x] 先收集小规模、有授权的制度/规范摘录及元数据（2 份公开法规、8 条释义，见 `knowledge/README.md`）。
- [x] 每条知识记录来源、版本、适用范围和更新时间（见 `knowledge/catalog.v1.json`）。
- [x] 实现只读 `search_knowledge` 工具（见 `docs/phase4-knowledge-contract.md`）。
- [x] 回答区分“模型建议”“检索依据”“人工结论”（后者只来自已关闭记录的复查事件）。
- [x] 找不到依据时明确说明，禁止生成虚假条款（引用只由目录组装，不接受模型或客户端填写）。
- [x] 固定案例评估检索命中、引用正确和过期材料处理（15 个冻结案例及专项错误矩阵）。

验收：引用可追溯；无来源时不伪造；知识库关闭时核心咨询仍能安全降级。

状态：Phase 4 实现清单已完成；本地关键词检索、确定性引用与降级、API 和三分区页面通过自动及
本地浏览器验收。资料的专业复核和项目实际适用性仍是人工门禁；自由模型建议不等于核验事实。
未引入向量库、图片、多智能体或真实/付费模型调用。

## Phase 5：单张图片上传与分析

教程依据：先遵循前述工具、结构化输出、人工确认和评估规范；图片是扩展能力。

- [x] 确认所选模型支持图片输入并完成独立连通性验证（独立VISION_MODEL，见 `docs/phase5-live-smoke.json`）。
- [x] 限制格式、大小、数量和存储位置，清理 EXIF 等不必要信息（见 `docs/phase5-image-contract.md`）。
- [x] 图片只作为证据之一，输出“观察到/不确定/未观察到”，不作最终定性（严格VisionAnalysis契约）。
- [x] 支持一张图片发现多个候选问题（最多5项，逐项人工采纳/驳回）。
- [x] 低清晰度、遮挡或缺少环境上下文时要求补拍/补充描述（尺寸门禁及结构化追问）。
- [x] 整改前后照片关联同一问题记录（关联时角色/阶段/revision校验，不推进状态）。
- [x] 建立图片专项测试集和人工复核表（12个冻结场景、合成图片测试、`docs/phase5-human-review.md`）。

验收：图片失败不影响文字上报；错误识别可纠正；高风险结论必须由人工确认。

状态：Phase 5 实现清单已完成；单图上传/脱敏、单Agent视觉适配、人工纠正、照片关联通过离线及
隔离浏览器验收，另完成一次明确授权的合成图真实冒烟。真实现场图片准确率、专业复核、生产身份
与部署安全尚未验收；Phase 5 验收时文字表单仍是结构化入口，后续产品补齐见下节。

## 产品补齐：文字入口与历史工单（Phase 6 前）

用户授权补齐真实文字入口和历史列表/详情；契约见 `docs/product-text-records-contract.md`。

- [x] 接通单轮真实文字回答及结构化分析，逐次外发确认。
- [x] 实现有界服务端追问上下文、高风险保留及最新分析提案。
- [x] 增加历史工单搜索、筛选、分页及可刷新详情。
- [x] 完成专项测试、浏览器闭环及边界文档。

状态：产品补齐实现完成；50项专项和349项完整测试、前端构建、隔离浏览器通过。
真实文字适配器以Fake SDK核对协议，本轮无真实/付费调用；真实文字连通性与现场判断效果仍待独立验收。
未完成生产身份/权限、双机安装或Phase 6指标，不将其算作完整上线验收。

## 产品体验重构：会话工作台（Phase 6 前）

契约见 `docs/conversation-workbench-contract.md`。

- [x] 冻结侧边栏信息架构、三种入口、会话状态迁移、工单出现条件与 W01–W10 场景。
- [x] 新增普通聊天、专业咨询、直接提交的独立路由与统一会话工作台。
- [x] 增加最近工单、全部历史、详情恢复、移动抽屉、图片入口和明确空/加载/错误状态。
- [x] 保留咨询资格门禁、人工确认、HMAC、幂等保存、六状态机与高风险独立专业复查。
- [x] 完成专项测试、完整验证和桌面/390×844 隔离浏览器验收。

状态：会话工作台体验重构完成；新增 5 项前端契约检查，完整 354 项 Python 测试和 Vue 生产构建通过。
浏览器使用隔离 Fake/Mock 服务，文字与图片真实模型调用均为 0。未新增 Agent、RAG、多智能体、依赖升级
或模型业务写权限；生产身份、权限与持久聊天历史仍不在本轮范围。

### 会话交互与日常聊天修订

目标：日常问题由轻量模型直接回答；发送即时显示，文字等待不锁住导航，移除重复发送确认。
教程依据：第7/9章最小对话和有界上下文、第12章行为评估、Extra09失败留痕。
当前行为：原聊天与咨询共用结构化分析；发送成功后才清空输入，文字忙碌状态阻止导航。
验收条件：消息先显示、输入清空；等待时切换工单且可编辑；旧回答不能回填新页面；失败输入可重试。
允许修改：文字意图区分、模型适配、会话交互、回归测试和相应文档。
禁止修改：冻结的 Phase 1/3 契约、正式工单状态权限、图片授权；不增加多 Agent 或生产账户。
测试命令：`powershell -ExecutionPolicy Bypass -File .\scripts\verify.ps1`；隔离 Fake 浏览器验收。
人工确认点：真实模型性能和日常回答质量需独立授权验收；专业咨询/工单仍执行人工审核。

- [x] 区分 `chat` 轻量回答和 `consult` 结构化分析，日期作为服务器上下文提供给模型。
- [x] 移除文字额外确认界面，保留显式发送动作与后端外发字段校验。
- [x] 请求前展示消息和清空输入；取消导航、卸载清理、失败重试和请求体读取超时均有回归测试。
- [x] 362项 Python、7项前端行为测试、生产构建及延迟 Fake 浏览器验证通过；真实模型调用0次。

### 局域网聊天卡住与执行状态修复

契约与变更门禁见 `docs/chat-progress-contract.md`。

- [x] Chrome DevTools定位局域网HTTP缺少randomUUID，补齐安全随机ID及发送准备异常恢复。
- [x] 等待显示实际经过秒数、服务端步骤、真实提案工具；计时器不伪造进度或模型思维。
- [x] 新增兼容的NDJSON状态端点，35秒等待上限覆盖完整正文；文字锁排队最多1秒。
- [x] 局域网桌面/390×844移动验收、错误恢复与全项目离线回归通过，真实模型调用0次。

全项目只读审计另外发现3项既有P2，按独立主题处理：

- [x] 工单动作说明已按500/1000/2000字目标字段预校验，超限稳定返回参数错误（本任务完成）。
- [x] 旧提案确认接口精确验证布尔值，现有回归已证明拒绝`confirmed: 1`（2026-08-31复核为过期待办）。
- [x] 缩短视觉模型调用持有PhotoStore全库锁的时间，避免图片读取/关联连带等待。

### PhotoStore 视觉调用锁范围修复（2026-08-31）

目标：视觉供应商等待期间不持有 PhotoStore 全库锁，同时保持同一图片分析串行、每图最多 10 次、
原子索引写入、审计和稳定错误码不变。
当前行为：`PhotoService.analyze` 在 `PhotoStore.mutate` 回调内读取图片、调用视觉模型并保存结果；
慢模型会阻塞无关图片读取、上传与关联。
验收条件：阻塞 Fake 模型运行时，同库图片读取可及时完成；同一图片第二个分析仍须等待，且并发不会
绕过 10 次容量上限；模型失败不写入分析；现有 Phase 5 API 与存储测试不回退。
允许修改：`PhotoStore` 的有界逐图分析锁、`PhotoService.analyze` 临界区、并发回归测试及对应文档。
禁止修改：视觉 Prompt/Schema、真实模型配置、图片授权和完整性规则、人工候选确认、工单状态机、前端。
测试命令：Phase 5 专项 pytest、`scripts/verify.ps1`、`git diff --check`；pytest 禁止真实/付费模型调用。
人工确认点：真实供应商延迟与多进程部署性能另行压测；本任务仅证明单进程本地服务的确定性并发边界。

状态：阻塞 Fake 视觉调用期间同库图片读取可在 0.5 秒门限内完成；同一图片分析仍逐个执行，
第 10/11 个并发请求只产生第 10 次模型调用并稳定返回 200/409。Phase 5 专项 68 项、全量
409 项 Python、30 项前端行为测试及生产构建通过，真实/付费模型调用 0 次。

### 极简消息与普通聊天提示词修订

契约：`docs/conversation-workbench-contract.md` 的 M01–M06；编辑指南：`docs/chat-prompt-guide.md`。

- [x] 移除消息头像、助手名称与轮数；已发送/等待中用户消息均靠右，助手纯文本靠左。
- [x] 用时作为折叠入口，实际步骤、工具与Mock说明按需查看；保留等待时间、风险及人工确认。
- [x] 普通聊天提示词集中为CHAT_SYSTEM_PROMPT，限制重复身份介绍/规则复述，按服务端配置回答型号。
- [x] 52项专项、370项完整Python测试、21项前端测试和构建通过；Chrome桌面/390×844验收通过。

真实模型调用0次；Fake SDK只证明提示词与配置正确发送，重复开场的改善仍需真实模型抽样。
独立分支`style/minimal-chat`依赖尚未合并的`fix/chat-progress`（PR #16），不自动合并。

### 普通聊天重复回答：逐轮上下文修复

契约：`docs/chat-turn-history-contract.md`，H01–H06。

- [x] 重现第三轮仍把历史问题和上轮回答放在一条user消息中的请求结构问题。
- [x] 改为服务端成功历史的user/assistant配对，当前输入单独置于末尾；专业咨询不变。
- [x] 约束最多5对历史，校验数量和答案长度；失败/重放不追加，新会话不串线。
- [x] 新增19项离线SDK/API回归；65项专项、389项Python全量、21项前端及生产构建通过。

未调用真实模型；重复率和回答质量需独立实际抽样。新分支`fix/chat-turn-history`基于PR #17，未自动合并。
历史输入增加至最多5条旧答案（每条≤1000字），费用/耗时仍受实际供应商影响，未测真实性能。

### 普通聊天深度思考与容量升级（2026-08-31）

契约：`docs/deep-chat-contract.md`，D01–D07；基线为已合并main 09b4f62。

- [x] 开启普通聊天thinking/high，输出预算32768、模型等待180秒；按问题复杂度回答，不强制短答。
- [x] 产品v2支持16000字输入/64000字回答/50轮，保留Phase1/3和v1快照；专业咨询限制不变。
- [x] 最近12对/120000字完整问答裁剪与context_trimmed提示；累计内容400000字门禁、幂等重放保护。
- [x] 页面190秒截止、停止等待/原请求重试；聊天区与业务表单禁用分离，长文本与手机布局通过。
- [x] 75项专项Python、400项全量Python、25项前端行为测试、生产构建与Chrome离线浏览器通过。

未改.env或密钥，真实文字/图片调用0次；耗时/费用/回答质量仍需用户主动发送后独立验收。
没有逐token输出、持久聊天或自动工单；停止等待不保证远端停算停费，长请求仍占用既有单进程文字锁。

### 聊天阅读与 Markdown 安全重构（2026-08-31）

契约：`docs/chat-visual-contract.md`，视觉 V01–V10、安全 S01–S06。

- [x] 普通聊天与专业咨询采用 800–900px 阅读轴、中文 17px 正文和舒适段落/列表节奏。
- [x] 保留右侧浅灰用户气泡；助手无头像、名称、卡片或底色，底部 composer 悬浮且与正文对齐。
- [x] 使用安全 Markdown 渲染标题、粗体、列表、代码和链接；关闭原始 HTML、远程图片和危险协议。
- [x] 保留长回答、16000字输入、停止等待、真实阶段、上下文裁剪、风险复核和受控提案入口。
- [x] 前端专项、完整验证及桌面/390×844 隔离浏览器验收通过，真实模型调用 0 次。

边界：不改 DeepSeek 运行时或 `CHAT_SYSTEM_PROMPT`，不引入 Codex harness、App Server、CLI、工具系统、
RAG、多智能体或自动工单；仅新增经许可证核验的 `markdown-it` MIT 依赖。

状态：401项Python、30项前端测试、Vue生产构建和隔离浏览器通过；桌面实测正文17px/1.78行高、
阅读轴860px，390×844无横向溢出。未改提示词或DeepSeek运行时，真实模型调用0次。

### 工作流动作说明长度校验（2026-08-31）

目标：让工作流动作在写入Store前按目标字段上限拒绝过长说明，稳定返回`invalid_arguments`，不落入500。
教程依据：Hello-Agents V1.0.3确定性状态与Pydantic边界；Extra09稳定错误和失败无副作用。
当前行为：请求统一允许2000字；request_more_info/supplement_information目标字段只允许500字，
reject_review/close目标字段只允许1000字，校验发生太晚并可能变为500。
验收条件：500/1000/2000精确边界通过预校验；各动作超1字在读取记录前400；短文本生命周期不回退。
允许修改：WorkflowTransitionRequest动作参数校验、Phase3 API回归、任务与验证记录。
禁止修改：状态迁移、角色权限、持久字段上限、前端聊天、模型/Prompt、依赖、.env和密钥。
测试命令：`pytest tests/test_phase3_api.py tests/test_phase3_contract.py tests/test_phase3_workflow.py -q`及`verify.ps1`。
人工确认点：无外部调用；前端后续可按动作显示剩余字数，但本任务不扩大为界面重构。

- [x] 冻结动作到字段的说明上限并建立超限失败基线。
- [x] 在请求边界返回400且不触发Store读取/写入。
- [x] 完成专项、完整离线验证并记录结果。

### 统一聊天、AI 工单交接、流式回答与模型选择（2026-08-31）

目标：合并新建聊天/咨询，由结构化 AI 判断是否进入对应类别工单申请；逐段展示已校验回答，并支持
服务端建议或用户填写的 DeepSeek 模型标识。
教程依据：第7章结构化输出与受控工具、第9章有界上下文、第12章固定验收、Extra09确定性状态。
当前行为：入口分离；咨询工单需停留原页点击；最终 answer 一次渲染；型号选择器不可操作。
验收条件：`docs/unified-chat-routing-contract.md` U01–U08。
允许修改：文字产品 v3、运行信息、NDJSON 展示事件、聊天/路由/工单交接、测试及文档。
禁止修改：工单状态机和权限、自动确认/保存/派工、图片/知识边界、多 Agent、密钥或未授权真实调用。
测试命令：专项 pytest、前端测试/构建、`scripts/verify.ps1`和隔离 Chrome 桌面/移动验收。
人工确认点：真实模型分类、回答质量、费用和自定义型号可用性需使用者另行抽样；工单仍须人工确认保存。

- [x] 合并侧栏和路由入口，保留旧`/consult`到`/chat`兼容跳转。
- [x] 新增`intent=auto`与产品 v3 请求快照，复用单次结构化 AI 判断。
- [x] `can_propose`四类分析自动打开对应工单申请并锁定 AI 分析，不自动调用工具或保存。
- [x] 新增经完整 Schema 校验后的有序回答 delta；非法/中断流失败关闭且不自动重发。
- [x] 模型选择器支持服务端建议列表与合法自定义标识，密钥和官方端点不可由页面覆盖。
- [ ] 完成专项、全量离线验证及 Chrome 桌面/390×844验收并记录证据。
  - [x] 426项Python、35项前端行为测试、`vue-tsc`与Vite生产构建通过；真实模型调用0次。
  - [ ] Chrome正在运行且原生主机正确，但Profile 4未安装浏览器扩展，桌面/移动验收未冒充通过。

## Phase 6：评估、演示和发布

教程依据：第 12、16 章。

### 离线评估与脱敏 Trace 契约（2026-08-31）

目标：以 20 条冻结文字案例生成可复现指标报告，并保存成功/失败 Trace；严格区分协议回放、真实模型
和人工裁决，缺失的 Token、费用或闭环数据必须显示未测量。
当前行为：已有 Fake 20 案例全通过测试，但没有统一指标口径、Trace Schema、阈值判定或可复现报告。
验收条件：分类、字段、风险、路由、工具、安全和工程指标均由严格 JSON 输入确定性计算；Trace 不含原文、
模型正文、隐藏推理或密钥；成功与失败样例可校验；默认验证零联网、零付费调用。
允许修改：新增 `evaluation` 纯计算模块、冻结 Schema/样例/报告、CLI、测试和 Phase 6 文档。
禁止修改：Agent/Prompt、模型配置、API/前端、工单状态机、知识库、`.env` 和密钥；不得把 Fake 分数
宣传为真实模型准确率或专业结论。
测试命令：Phase 6 专项 pytest、报告重生成比对、`scripts/verify.ps1`、`git diff --check`。
人工确认点：真实模型样本需另行费用授权并由专业人员标注；双机与浏览器发布验收属于后续独立主题。

- [x] 指标：分类准确率、字段完整率、风险召回、工作流建议正确率、工具调用正确率。
- [x] 安全指标：虚假引用、高风险漏报、越权状态变更、隐私泄漏。
- [x] 工程指标：响应时间、失败率、Token/调用成本、任务闭环率。
- [x] 保存成功和失败 Trace，形成可复现验证报告。
- [ ] 完成 Windows 双机安装测试和浏览器全流程验收。
  - [x] 当前 Windows 11 开发机从全新克隆完成 `setup.ps1` 和 `verify.ps1`，无密钥/运行数据复制。
  - [ ] Chrome DevTools MCP 新建/列出页面持续无响应，当前机浏览器全流程未冒充通过。
  - [ ] 另一位队员的物理 Windows 电脑尚未执行和签字。
- [x] 完成 README、演示脚本、已知限制和比赛展示材料。

验收：固定测试与人工验收通过，另一位队员从 `main` 新克隆后能够复现。

离线评估状态：20 条合成协议回放通过全部行为与安全门禁；另保存 1 条高风险模型超时失败 Trace，
其报告按预期未通过高风险召回门禁。工程指标计算器已覆盖响应时间、失败率、Token、费用和闭环率；
本次无供应商 usage/账单或真实闭环裁决，后三项明确为 `not_measured`，不填 0 冒充实测。该结果不代表
真实模型准确率；真实模型与专业人工验收仍是独立门禁。

### Phase 6 发布验收包（2026-08-31）

目标：让队员可从全新克隆复现安装/验证，并用统一脚本演示咨询、提案、人工确认、六状态整改、知识和
图片降级；比赛材料准确区分已完成能力与上线前门禁。
当前行为：README有安装与功能说明，但没有全新克隆自动验收器、双机签字表、逐步演示稿、发布检查表
或集中已知限制；既有浏览器记录分散在各阶段验证中。
验收条件：本机从显式仓库/分支全新克隆后setup+verify通过且不复制`.env`/运行数据；隔离Fake浏览器
完成普通聊天、专业咨询到关闭、历史恢复、知识和Mock图片路径；桌面/移动端无横向溢出或控制台错误；
文档不声称真实模型/身份/跨进程/双机已验收。
允许修改：安全的全新克隆验证脚本、README、演示脚本、发布检查表、已知限制、比赛展示材料和验证记录。
禁止修改：Agent/Prompt、模型配置、API/前端业务逻辑、Schema、工作流权限、依赖、`.env`和密钥；
不调用付费模型，不伪造第二台物理电脑或专业验收签字。
测试命令：本机全新克隆setup+verify、原仓`verify.ps1`、隔离Fake浏览器全流程、`git diff --check`。
人工确认点：第二位队员在另一台Windows电脑填写验收表；真实模型/现场资料由获授权专业人员验收。
