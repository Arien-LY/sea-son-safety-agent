# AGENTS.md

本文件适用于整个 `sea-son-safety-agent` 仓库。所有人类开发者、Codex 及其他编码 Agent
在修改前必须阅读本文件。

## 产品目标

建设一个面向施工现场的单 Agent 入口，覆盖：

- 安全隐患：临时用电、临边防护、消防通道、个人防护等。
- 质量问题：开裂、渗漏、空鼓、尺寸偏差、成品损坏等。
- 管理问题：材料堆放、标识、记录、违规行为等。
- 后勤问题：宿舍、供电、漏水、卫生、空调和门锁等。

Agent 负责理解、提取、追问、检索和生成待审核建议；程序负责状态、权限、持久化和确认；
专业人员负责最终工程判断和复查关闭。

## Hello-Agents V1.0.3 强制规范

本项目必须遵循仓库内 `docs/tutorial-compliance.md`。核心要求：

1. 从最小可运行的单 Agent 闭环开始，不提前建设多智能体。
2. 先完成基础对话，再加入工具；复杂能力必须逐阶段通过验收。
3. 工具优先采用原生 Function Calling，禁止依赖脆弱的自然语言正则协议作为正式路径。
4. 模型输出必须通过 Pydantic/JSON Schema 校验，禁止直接信任自由文本字段。
5. 每个工具单一职责、参数明确、返回统一，工具数量保持最小。
6. 模型不能直接改变业务状态，只能提交受控提案，由确定性代码验证和执行。
7. 安全红线和人工确认必须位于不可被会话压缩丢失的代码或系统层。
8. 保留调用、结果、状态变化和错误摘要；不记录或展示隐藏思维链。
9. 默认测试使用 Fake/Mock，不调用收费模型；真实模型测试单独执行。
10. 先定义固定案例和验收指标，再扩展 RAG、图片、多智能体等能力。

对应教程章节：第 4 章 Agent 范式、第 7 章框架与工具、第 8 章记忆/RAG、第 9 章上下文、
第 12 章评估、第 16 章毕业项目，以及 Extra09 的最小闭环和可观测性原则。

## 架构边界

```text
frontend/       输入和展示，不拥有业务状态
backend/app/    API、状态机、持久化、上传和权限
agents/         模型适配、结构化分析、Prompt 和受控工具
knowledge/      有来源、适用范围和更新时间的知识材料
tests/          确定性单元/API/契约测试
docs/           架构、决策、教程映射和验收记录
```

- 不把 Prompt、模型循环和工具实现写进 API 路由。
- 不由前端计时器伪造 Agent 进度。
- 不把数据库状态、模型判断和页面状态设置为多个事实来源。
- 不把 Codex CLI/App Server 当作 V0 产品运行时；依据见 ADR 0001。

## 开发顺序门禁

严格按照根目录 `TASKS.md` 推进。上一阶段验收未通过，不得进入下一阶段：

```text
文字咨询 → 结构化分析 → 受控任务工具 → 整改工作流 → 知识检索 → 图片识别
```

## 安全红线

- 禁止提交 API Key、Token、密码、Cookie、真实个人信息和未经授权的项目资料。
- 不根据单张照片作最终安全、质量或责任认定。
- 高风险和信息不足时必须明确不确定性并转人工。
- 禁止自动处罚、自动归责、自动关闭高风险问题或执行不可逆外部操作。
- 不伪造规范条文、来源、图片内容、工具结果或测试结果。

## 任务模板

每个实现任务至少写明：

```markdown
目标：
教程依据：
当前行为：
验收条件：
允许修改：
禁止修改：
测试命令：
人工确认点：
```

## Git 与验证

- 不直接在 `main` 开发；一个分支和 PR 只处理一个主题。
- 修改前读取相关契约、调用方、测试和文档。
- 不夹带格式化、依赖升级、重命名和无关清理。
- 未经明确要求，不自动提交、推送、创建或合并 PR。
- 完成前运行：

```powershell
git status
git diff
powershell -ExecutionPolicy Bypass -File .\scripts\verify.ps1
```

- PR 必须写明教程依据、修改内容、验证结果、已知限制和人工评审重点。

