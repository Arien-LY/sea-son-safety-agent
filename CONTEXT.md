# 安全质量咨询

本上下文统一施工现场安全、质量、管理、后勤咨询入口使用的领域语言，避免把对话输入与受控业务记录混为一谈。

## Language

**文字咨询输入**：
用户发送的一条纯文字消息，可以表达一般咨询或现场问题描述；它不是待上报的问题表单，也不表示业务状态发生变化。
_Avoid_: 问题表单、上报记录

**咨询者角色**：
用户为获得合适表达方式而自报的身份背景；它不授予权限，也不表示责任主体已经确定。
_Avoid_: 责任人、权限角色

**项目标签**：
用户提供的项目名称文字，用于补充咨询语境；它未经系统核实，不是正式项目标识，也不参与权限判断。
_Avoid_: 项目 ID、授权项目

**区域标签**：
用户提供的现场位置文字，用于帮助理解消息；它未经系统核实，不是正式区域标识，也不能单独触发派单。
_Avoid_: 区域 ID、派单位置

**主类别**：
一条文字咨询输入唯一的业务归口，可以是普通咨询、安全、质量、管理、后勤或未知；它与风险等级相互独立。
_Avoid_: 风险类型、严重程度

**未知类别**：
文字不足、含义不清或无法确定唯一主类别时使用的保守归口；它不表示问题不存在，也不能用猜测替代。
_Avoid_: 其他、默认类别

**风险等级**：
依据用户明确提供的事实，对潜在后果严重性和处置紧迫度的分级；它与主类别和分析置信度相互独立。
_Avoid_: 问题类别、置信度

**风险未确定**：
现有文字不足以判断风险时使用的保守等级；它不等于低风险，也不能通过乐观猜测降级。
_Avoid_: 低风险、无风险

**紧急风险**：
危险正在发生或人员处于立即暴露、被困等紧迫状态的最高风险等级。
_Avoid_: 一般高风险、所有严重问题

**缺失字段**：
完成当前分析仍需要用户补充的明确信息；它必须具体可询问，不能用模型猜测的位置、人员、状态或责任
代替。
_Avoid_: 推断事实、默认地点

**立即行动**：
高风险或紧急风险下，为降低人员暴露和阻止危险扩大的临时避险提示；它不是最终工程处置方案，也不
授权无资质人员靠近、触碰、带电测试、拆卸或维修危险源。
_Avoid_: 整改方案、自动处置

**人工复核**：
要求有资格的现场人员核实事实、风险和后续处置的结构化标记；它不代表已经确认责任，也不产生派单、
处罚、关闭或其他业务状态变化。
_Avoid_: 自动审批、最终定责

**问题记录提案**：
由受控工具根据结构化问题分析生成、等待用户补充和确认的数据建议；它不是正式问题记录，不具有记录
编号，也不表示已经保存、派单或进入整改工作流。
_Avoid_: 问题记录、已上报工单

**用户确认**：
用户核对并接受提案内容的显式动作；确认之前提案只能展示和修改，不能产生持久化或派单副作用。
_Avoid_: 模型确认、自动提交

**记录展示字段**：
用户可在提案中补充或修订的标题、描述、项目、区域和说明；它们用于后续记录展示，不能覆盖模型分析
中的风险、路由、人工复核和已观察事实。
_Avoid_: 风险修订、责任认定

**确认待持久化**：
用户已经确认提案内容，但系统尚未创建正式记录、分配编号或派单的临时结果状态。
_Avoid_: 已上报、已建单

**正式问题记录**：
经用户确认、完整性校验并由确定性服务写入本地 Store 的业务记录；它具有正式记录编号、revision 和
状态轨迹，但本地演示记录不代表已经同步到外部企业系统。
_Avoid_: 问题记录提案、外部工单

**工作流主状态**：
正式问题记录在 `draft/submitted/assigned/rectifying/pending_review/closed` 六个值之一的确定性阶段；
状态只能由服务端权限矩阵推进，不能由 Agent 或浏览器直接指定结果。
_Avoid_: 模型计划步骤、页面显示状态

**记录处置标记**：
独立于主状态的 `active/cancelled` 标记；取消时保留当时主状态以便审计，并终止后续推进，不把
`cancelled` 偷偷增加为第七个主状态。
_Avoid_: 删除记录、关闭通过

**责任角色建议**：
根据已校验问题类别给协调员的归口参考；建议不填写责任人、不产生派工，也不能替代人的最终归责。
_Avoid_: 自动归责、已派工

**人工确认派工**：
协调员显式选择整改人和责任角色后产生的业务动作；只有此动作可以把记录推进到 `assigned`。
_Avoid_: Agent 派单、建议即指派

**记录 revision**：
正式记录每次成功变化递增的并发版本号；调用方必须基于最新 revision 操作，旧版本请求确定性冲突。
_Avoid_: 页面计数、时间戳替代版本

**工作流事件**：
每次创建或状态动作留下的业务轨迹，包含顺序、时间、动作人、角色、前后状态/处置和必要说明；它不
包含隐藏思维链或 Agent 内部上下文。
_Avoid_: 思维链日志、自由覆盖历史

**工具审计事件**：
工具调用产生的脱敏技术记录，只包含工具名、参数键名与摘要哈希、结果摘要、时间和错误码；不保存用户
原文、隐藏思维链或完整模型上下文。
_Avoid_: 对话日志、思维链日志

**候选检索依据**：
从已核验目录按地区、期限和关键词检索得到的资料摘编；保留来源、版本和条号，命中不等于确认
现场违法、合规或责任。摘编不是逐字原文，也不是模型生成的法规。
_Avoid_: AI 法律结论、命中即适用

**知识复核截止日**：
资料必须再次进行内部核对的日期；到日停止返回引用，不能解释成法规的法定失效日期。
_Avoid_: 法规失效日、自动确认现行有效

**人工复查结论**：
来自正式记录关闭事件的复查说明，附操作者、角色、时间和 revision；未经复查只能显示尚无结论。
本地演示角色尚不代表生产身份已认证。
_Avoid_: 模型自评、引用即复查通过
